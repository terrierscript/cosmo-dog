import { SearchInput } from "../Search";
import React from "react";
import { Gradient } from "../../grid/Background";

export default {
  component: () => {
    return (
      <Gradient>
        <SearchInput />
      </Gradient>
    );
  }
};
