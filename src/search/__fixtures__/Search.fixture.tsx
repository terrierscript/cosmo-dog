import { SearchInput } from "../Search";
import React from "react";
import { Gradient } from "background/Background";

export default {
  component: () => {
    return (
      <Gradient>
        <SearchInput />
      </Gradient>
    );
  }
};
