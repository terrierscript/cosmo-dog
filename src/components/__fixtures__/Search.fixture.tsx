import { SearchInput } from "../Search";
import { Gradient } from "../Background";
import React from "react";

export default {
  component: () => {
    return (
      <Gradient>
        <SearchInput />
      </Gradient>
    );
  }
};
